# Portable-Password-Manager-in-Python
A simple CLI-based password manager built with Python that lets users securely store, view, update, and delete credentials using encryption. Supports user authentication, admin controls, and a basic session system backed by SQLite.
